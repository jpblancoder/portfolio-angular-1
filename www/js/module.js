(function () {
    angular.module('port', [
        'ngAnimate',        // Provides support for CSS-based animations (keyframes and transitions).
        'ui.router',        // The de-facto solution to flexible routing with nested views in AngularJS
        'angular.filter',   // Bunch of useful filters for AngularJS (with no external dependencies!)
        'templates'         // Concatenates and registers AngularJS templates in the $templateCache.
    ]);
})();

